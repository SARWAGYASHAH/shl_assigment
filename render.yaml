services:
  - type: web
    name: shl-assessment-recommender
    runtime: docker
    plan: free
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: PORT
        value: 8000
    healthCheckPath: /health
    initialDeployHook: echo "Starting SHL Assessment Recommender..."
